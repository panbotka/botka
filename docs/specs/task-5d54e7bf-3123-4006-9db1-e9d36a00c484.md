Review the Help page and update it to reflect the current state of the app. The content may be outdated — features were added/changed since it was written.

## Requirements

### Audit the current Help page
- Read `frontend/src/pages/HelpPage.tsx` (or wherever the help content lives)
- Compare every section against the actual current app functionality
- Check for:
  - Features mentioned in help that no longer exist or work differently
  - Features that exist in the app but are not mentioned in help
  - Incorrect instructions or screenshots references
  - Outdated terminology (e.g. if things were renamed during the Saiduler+Chatovadlo merge)

### Update content to match current app
- Update descriptions of all features to match current behavior
- Add sections for any new features not yet documented:
  - Voice input (Whisper transcription)
  - File uploads/attachments
  - Personas and tags
  - App memories
  - Task scheduling and monitoring
  - Cost dashboard / API usage
  - Settings page options
  - MCP integration
  - PWA installation
  - Session pool / model selection
  - Project management
- Remove references to features that don't exist
- Fix any broken links or incorrect navigation paths

### Keep it concise
- Help should be scannable — short sections with headers
- Use the app's actual UI labels and navigation names
- Don't over-explain — users can explore, just point them in the right direction

## Implementation Notes
- Read the current HelpPage component first
- Cross-reference with the actual pages and features by reading the relevant components
- Keep the same component structure/styling — just update the content
- Existing tests must pass (`make check`)

## Safety
**NEVER run `make deploy`, `make install-service`, `systemctl restart botka`, or any command that would restart the Botka service.**