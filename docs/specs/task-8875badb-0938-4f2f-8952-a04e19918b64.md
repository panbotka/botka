# Fix active button contrast in dark mode

Active navigation buttons have poor contrast in dark mode — white/light background with unreadable text.

## Requirements

- Fix the **desktop sidebar NavLink** active state in `frontend/src/App.tsx` (line 59): currently `bg-zinc-200 text-zinc-900` with no dark mode variants
- Fix the **desktop sidebar NavLink** inactive/hover states in `App.tsx` (line 60): `text-zinc-600 hover:bg-zinc-100 hover:text-zinc-900` — also needs dark variants
- Fix the **mobile BottomNav** in `frontend/src/components/BottomNav.tsx`: the container (`bg-zinc-50 border-zinc-200` at line 24) and active/inactive button text colors (line 34) need dark variants
- Active state in dark mode should use a dark-appropriate highlight (e.g. `dark:bg-zinc-700 dark:text-zinc-100`)
- Inactive state should have readable text on dark backgrounds
- Ensure hover states also work in dark mode
- Do NOT change any other components — scope is limited to sidebar nav and bottom nav active/inactive states