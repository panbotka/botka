Add `cancelled` as a valid transition target from the `failed` status in all three places:

1. **`internal/handlers/task.go`** — `allowedTransitions` map, line ~45: change `models.TaskStatusFailed: {models.TaskStatusQueued: true}` to `models.TaskStatusFailed: {models.TaskStatusQueued: true, models.TaskStatusCancelled: true}`

2. **`internal/mcp/tools.go`** — `allowedTransitions` map, line ~22: same change as above.

3. **`frontend/src/components/TaskList.tsx`** — `statusTransitions` object, line ~51: change `failed: [{ label: 'Requeue', target: 'queued' }]` to `failed: [{ label: 'Requeue', target: 'queued' }, { label: 'Cancel', target: 'cancelled' }]`

4. **Tests** — Add test case `{"failed to cancelled", models.TaskStatusFailed, models.TaskStatusCancelled, true}` to the transition table in `internal/mcp/tools_test.go`.

Run `make check` to verify everything passes.