## Requirements

Reduce the main navigation menu by relocating items:
1. **Projects** → move under **Settings** (as a sub-page or tab)
2. **Costs** → move to **Dashboard** (main page) as a section/widget

Remove both items from the main navigation menu after relocation.

## Implementation

### 1. Projects under Settings
- Find the navigation/menu in `frontend/src/` — likely a sidebar/navbar component
- Remove "Projects" from the main menu
- Add Projects as a tab or section on the Settings page (`frontend/src/pages/`)
- Preserve all existing functionality (CRUD for projects, branch strategy settings, verification command, etc.)
- Update routing — either `/settings/projects` as a nested route, or a tab on `/settings`

### 2. Costs on Dashboard
- Remove "Costs" from the main menu
- Add a costs widget/section to the Dashboard page
- Display key cost information directly on the dashboard (daily/monthly costs, chart)
- Preserve existing costs functionality, just relocate it

### 3. Menu cleanup
- After relocation, the menu should have fewer items
- Ensure no links to old routes are broken (add redirects or update references)

## Where to look

- Navigation/menu: `frontend/src/components/` — search for sidebar, navbar, navigation, menu
- Pages: `frontend/src/pages/` — Settings, Dashboard, Projects, Costs pages
- Router: `frontend/src/` — route definitions (React Router)
- API: backend endpoints for projects and costs remain unchanged