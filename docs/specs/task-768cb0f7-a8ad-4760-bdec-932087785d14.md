# Fix missing assistant avatar in chat

The assistant avatar in the chat UI shows a broken image. The `AssistantAvatar` component references `/avatar-assistant.jpg` but the file doesn't exist in `frontend/public/`.

## Requirements

- Copy `/mnt/nas-botka/images/signal-chat/panbotka-work.jpg` to `frontend/public/avatar-assistant.jpg`
- Resize/optimize the image to a reasonable avatar size (e.g. 128x128 or 256x256) since the original is 1024x1024 and the component renders it at 28x28px
- Verify the `AssistantAvatar` component in `frontend/src/components/MessageBubble.tsx` correctly references the image (it already does at line 58)
- The avatar should display as a rounded circle next to assistant messages

## Implementation Notes

- The component already has the correct `src="/avatar-assistant.jpg"` and `alt="Pan Botka"` — only the image file is missing
- Use `convert` or `ffmpeg` to resize if available, otherwise copy as-is