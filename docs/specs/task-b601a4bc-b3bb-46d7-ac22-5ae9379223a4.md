# Include spec file in task agent commit

Task agents should always commit the specification file (`docs/specs/task-{uuid}.md`) together with their implementation changes.

## Requirements

- In `internal/runner/executor.go`, update the `buildPrompt()` method to instruct the agent to include the spec file in its commit
- Add to the prompt something like: "Include the spec file docs/specs/task-{uuid}.md in your commit."
- The spec file path is already referenced in the prompt (`docs/specs/task-%s.md`), so just add the instruction to commit it alongside other changes
- Do NOT change anything about how `syncSpec()` works — the file write before execution is correct
- `make check` must pass

## Implementation Notes

- The change is in `buildPrompt()` at `internal/runner/executor.go:119-134`
- Keep the prompt concise — just append the instruction to the existing prompt string
- Update any tests that assert on the prompt content