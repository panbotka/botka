## Task 7: Task Scheduler Frontend Pages

Port Saiduler's frontend pages for Dashboard, Tasks, Task Detail, and Projects to Botka.

### Context
These are the task scheduling UI pages from Saiduler. They should replace the placeholder pages created in Task 3. Keep Saiduler's exact design system (zinc/emerald/red/amber colors).

### Reference Files
- `/home/pi/projects/saiduler/frontend/src/pages/DashboardPage.tsx`
- `/home/pi/projects/saiduler/frontend/src/pages/TasksPage.tsx`
- `/home/pi/projects/saiduler/frontend/src/pages/TaskDetailPage.tsx`
- `/home/pi/projects/saiduler/frontend/src/pages/ProjectsPage.tsx`
- `/home/pi/projects/saiduler/frontend/src/pages/SettingsPage.tsx`
- `/home/pi/projects/saiduler/frontend/src/components/` — RunnerStatus, TaskForm, TaskList, LiveOutput
- `/home/pi/projects/saiduler/frontend/src/hooks/` — useSSE, useTasks, useProjects, useRunnerStatus
- `/home/pi/projects/saiduler/frontend/src/App.tsx` — Route setup reference

### What to Port

1. **Pages:**
   - `DashboardPage.tsx` — Runner status card, API usage meters (5h/7d), active tasks, recent results
   - `TasksPage.tsx` — Task list with status filters, batch operations, bulk select, drag-drop reorder via dnd-kit
   - `TaskDetailPage.tsx` — Task detail with execution history table, live output streaming, retry button
   - `ProjectsPage.tsx` — Project list with task counts, click to edit settings (branch_strategy, verification_command, claude_md, sort_order)

2. **Components:**
   - `RunnerStatus.tsx` — Runner state indicator with start/pause/stop buttons, task counter
   - `TaskForm.tsx` — Create/edit task form (title, spec, project selector, priority)
   - `TaskList.tsx` — Sortable task list with status badges, drag handles, batch selection
   - `LiveOutput.tsx` — Real-time task output display via SSE

3. **Hooks:**
   - `useSSE.ts` — SSE connection for task output streaming
   - `useTasks.ts` — Fetch/manage task list with filtering and pagination
   - `useProjects.ts` — Fetch/manage project list
   - `useRunnerStatus.ts` — Poll runner status with auto-refresh

4. **Update App.tsx** — Replace placeholder routes with real page components. Make sure the sidebar highlights the active section.

5. **Update api/client.ts** — Add all task and runner API methods if not already present.

### Important Notes
- Copy the components as closely as possible from Saiduler
- Update API base path if Saiduler uses different paths (both should use `/api/v1/`)
- Ensure dnd-kit drag-and-drop works for task reordering
- Ensure SSE streaming works for live task output

### Verification
- Dashboard shows runner status and usage meters
- Tasks page lists tasks with correct status colors
- Can create a task via the form
- Can drag-and-drop reorder tasks
- Task detail page shows execution history
- Live output streams in real-time when a task is running